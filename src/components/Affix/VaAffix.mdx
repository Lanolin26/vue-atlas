import { Meta } from '@storybook/blocks';
import * as VaAffix from './VaAffix.stories'

<Meta of={VaAffix} />

# Affix

Компонент, используемый в качестве оболочки для окружения элемента для прикрепления к определенному месту.

## Component options

### Props

|  name  | type           | default | required     | description                                                          |
|--------|----------------|---------|--------------|----------------------------------------------------------------------|
| offset | number, string | 0       | not required | Расстояние вертикального смещения, к которому необходимо прикрепить. |

### Events

### Methods
